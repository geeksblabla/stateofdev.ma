name: Validate Survey Questions

on:
  pull_request:
    paths:
      - "survey/**"
  push:
    branches:
      - master
      - main
    paths:
      - "survey/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: 📦 Setup Node + PNPM + install deps
        uses: ./.github/actions/setup-node-pnpm-install

      - name: 🚑 Run Validate Episode Markdown
        run: |
          chmod +x .github/scripts/validate-survey-questions.sh
          .github/scripts/validate-survey-questions.sh

      - name: Save PR number
        if: github.event_name == 'pull_request' && always()
        run: |
          mkdir -p ./pr
          echo ${{ github.event.number }} > ./pr/NR
          cp .astro/survey-questions-validation.md ./pr/validation-report.md

      - name: Upload validation results
        if: github.event_name == 'pull_request' && always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-validation-data
          path: pr/