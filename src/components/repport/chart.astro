---
import { getQuestion } from './utils';
import {BarChart} from './bar-chart'
import type { Year } from './data';

export interface Props   {
  id: string;
  year: Year
}
const { id = "", year = "2020" } = Astro.props;

const results = getQuestion({ id, year });
---

<BarChart  results={results} />
