import React from "react"
import Results from "../components/Results/index"
import { Layout } from "../components"

export default () => {
  return (
    <Layout>
      <Results />
    </Layout>
  )
}
